function spotlight(game) {
  // console.log("Spotlight");
  // const spotlight = game.make.sprite({
  //   x: 100,
  //   y: 200,
  //   key: 'mask',
  //   add: false
  // });
  //
  // game.player.mask = new Phaser.Display.Masks.BitmapMask(game, spotlight);
}

function move(pointer, x, y) {

  mask.x = x - 100;
  mask.y = y - 100;

}